using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CubeBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Veljko Trajkovic");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Blue");
    }
}
